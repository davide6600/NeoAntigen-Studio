params {
    outdir = 'results'
    job_id = 'job-unknown'
    input_manifest = ''
}

process {
    executor = 'local'
    cpus   = 1
    memory = '2 GB'
    time   = '1h'
}

profiles {
    test {
        process.executor = 'local'
    }
}
